$kind: http-request
name: 'Get the latest T&C version'
description: |-
  Returns the T&C version with the highest `id`. This is the version the app should present to customers on login.

  **Returns:** the latest T&C object with `id`, `content`, and `publishedOn`.
method: GET
url: '{{baseUrl}}/terms-and-conditions/latest'
order: 2000
